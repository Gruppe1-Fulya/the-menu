spring.datasource.url=jdbc:postgresql://localhost:5432/${MENU_DBNAME}
spring.datasource.username=${MENU_USERNAME}
spring.datasource.password=${MENU_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
